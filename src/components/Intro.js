import React from 'react';

import './Intro.scss';

export const Intro = () => {
    return (
        <div id="intro__hero">
            <h1>
                The agency for what comes next.
            </h1>
        </div>
    )
}

export default Intro;